<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Event time{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp"></script>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>
        <header class="w-[100vw] bg-slate-100 h-[10vh] px-10 mx-auto mb-6 border-b flex justify-between items-center">
            <h1 class="text-2xl text-black font-semibold">Event Time</h1>
            <ul class="flex gap-8 text-lg">
                <li><a class="pb-1 border-black hover:border-b-2 hover:border-b" href="{{ path('home') }}">Accueil</a></li>
                <li><a class="pb-1 border-black hover:border-b-2 hover:border-b" href="{{ path('list') }}">Evénements</a></li>
                <li><a class="pb-1 border-black hover:border-b-2 hover:border-b" href="{{ path('create') }}">Créer un événement</a></li>
                <li><a class="pb-1 border-black hover:border-b-2 hover:border-b" href="#">Hello</a></li>
            </ul>
        </header>

        {% block body %}{% endblock %}

        <footer class="w-[100vw] bg-slate-100 h-[10vh] px-10 mx-auto mt-6 border-t flex justify-between items-center">
            <p class="text-center w-full">Tous droits réservés</p>
        </footer>
    </body>
</html>
